# A forest is a directory timberfs searches for stores by handle.
# Edit DIR to move the default, drop another *.conf here to add one,
# or delete this file to disable handle lookup in /var/log/timberfs.
DIR=/var/log/timberfs
