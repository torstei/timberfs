# Runtime directory for timberfs log-intake FIFOs: timberfs-log@.socket creates
# /run/timberfs/<instance>.pipe in here. Installed as /usr/lib/tmpfiles.d/timberfs.conf
# and applied at boot; for an immediate first use run `systemd-tmpfiles --create`.
d /run/timberfs 0755 root root -
